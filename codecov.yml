coverage:
  status:
    project:
      default:
        target: auto
        threshold: 2%
    patch:
      default:
        target: 0%
        threshold: 2%

comment:
  layout: "condensed_header, condensed_files, condensed_footer"
  behavior: default
